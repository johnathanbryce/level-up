**hash map** -- data structure that stores key -> value pairs with one critical property: lookup, insert, and delete are all O(1) on average

Mental model "If I am about to write a nested loop where the inner loop is searching for something, ask: can I replace the loop with a hash map lookup?"