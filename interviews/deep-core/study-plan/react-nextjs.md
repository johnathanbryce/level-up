# React / Next.js (Area 5 — HIGH LEVEL ONLY)

> John is strong here — this is reflex-refresh, not teaching. Do not go deep. High-level spoken fluency only.

## Rendering model (re-render, reconciliation, memoization)

## Server vs Client Components (App Router)

## Data fetching + rendering strategies (SSR / SSG / ISR / streaming)

## Cesium-in-Next reality (one-liner)

_Cesium = heavy client-side WebGL → Client Component, dynamic-import with `ssr: false`._
